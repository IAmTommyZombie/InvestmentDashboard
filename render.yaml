services:
  - type: web
    name: investment-dashboard-scraper
    env: node
    buildCommand: |
      cd server
      npm install --production
    startCommand: node server/index.js
    envVars:
      - key: NODE_VERSION
        value: 18.x
    plan: free
